{
  "name": "maha-outcomes-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "frontend-v2",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend-v2",
    "build": "npm run build --workspace=frontend-v2",
    "build:all": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  }
}
